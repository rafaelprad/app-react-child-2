import React from "react";
import ReactDOM from "react-dom";
import AppChild2 from "./App";

ReactDOM.render(
  <React.StrictMode>
    <AppChild2 />
  </React.StrictMode>,
  document.getElementById("root")
);